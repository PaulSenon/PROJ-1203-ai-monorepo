/**
 * Add in memory reactive query websocket caching to convex query hooks
 * e.g. when a query is used during session, even if unmounted, it stays in sync
 *      in the background for instant fresh data when re-mounted
 */
