#!/bin/sh

if [ "${OPENCODE:-0}" != "1" ] && [ "${AGENT:-0}" != "1" ]; then
  exit 0
fi

repo_root=$(git rev-parse --show-toplevel)
cd "$repo_root" || exit 1

pnpm run lint:fix:staged
status=$?

if [ $status -ne 0 ]; then
  exit $status
fi

git update-index --again
