2026-02-11 - ui-last-assistant-min-height - PRD 7.1
- done: active-thread state contract added for reserve logic in provider
- decisions: `undefined` liveStatus treated unsettled; settled only `completed|error|cancelled`; submit latch per thread via `lastSubmittedThreadUuid`
- entropy: removed one debug log (`onFinish`) from chat active hook wiring
- files: `apps/web/src/hooks/use-chat-active.tsx`
- blockers/notes: repo has pre-existing type errors outside scope (`message footer`, `header route type`, `packages/infra/alchemy.run.ts`); next iter wire booleans into conversation + messages-list (PRD 7.2/7.3)

2026-02-11 - ui-last-assistant-min-height - PRD 7.2/7.3
- done: wired reserve boolean from conversation adapter to layout + messages list; applied assistant-only `last:min-h-[calc(100vh-20rem)]`
- decisions: keep rule render-time only (`hasSubmittedInActiveThread || !isThreadSettled`), no effect toggles; keep layout/list prop optional default false for demo compatibility
- entropy: removed unnecessary fragment in `ConversationMessagesList` (return mapped array directly)
- files: `apps/web/src/components/chat/conversation/conversation.tsx`, `apps/web/src/components/chat/conversation/conversation-layout.tsx`, `apps/web/src/components/chat/conversation/_parts/messages-list.tsx`
- blockers/notes: typecheck still fails on pre-existing out-of-scope errors (`message footer` unused var, `packages/infra/alchemy.run.ts` import extension); reserve applies once assistant row exists (last-child assistant rule), so pre-token reserve still depends on optimistic assistant-shell track; optional followup tighten prop to required after demo wiring audit

2026-02-11 - ui-last-assistant-min-height - QA controls followup
- done: added demo control for `shouldReserveLastAssistantSpace` in conversation test page and wired prop into `ChatConversationLayout`
- decisions: reset demo now also resets reserve toggle to false
- files: `apps/web/src/routes/components/_components/conversation.tsx`
- blockers/notes: typecheck unchanged, still fails on pre-existing out-of-scope errors (`message footer` unused var, `packages/infra/alchemy.run.ts` import extension)

2026-02-11 - ui-last-assistant-min-height - prop contract hardening
- done: made `shouldReserveLastAssistantSpace` required in conversation layout/list prop chain
- decisions: removed optional defaults to prevent silent false fallback if wiring regresses
- files: `apps/web/src/components/chat/conversation/conversation-layout.tsx`, `apps/web/src/components/chat/conversation/_parts/messages-list.tsx`
- blockers/notes: typecheck unchanged; same pre-existing out-of-scope errors (`message footer` unused var, `packages/infra/alchemy.run.ts` import extension)

2026-02-11 - ui-last-assistant-min-height-v2-erratum - rollback v1 hook leakage
- done: removed `hasSubmittedInActiveThread` + `lastSubmittedThreadUuid` from active-thread hook; kept only `isThreadSettled`
- decisions: reserve memory moved out of `use-chat-active`; keep business hook lean per v2
- files: `apps/web/src/hooks/use-chat-active.tsx`

2026-02-11 - ui-last-assistant-min-height-v2-erratum - L3 latch migration
- done: `conversation.tsx` now passes only `uuid` + `isThreadSettled`; layout keyed by `uuid`
- done: `conversation-layout.tsx` now owns reserve latch with guarded render-phase sync, then derives `shouldReserveLastAssistantSpace`
- done: `messages-list.tsx` unchanged algorithm, still assistant-only `last:min-h-[calc(100vh-20rem)]`
- files: `apps/web/src/components/chat/conversation/conversation.tsx`, `apps/web/src/components/chat/conversation/conversation-layout.tsx`, `apps/web/src/components/chat/conversation/_parts/messages-list.tsx`

2026-02-11 - ui-last-assistant-min-height-v2-erratum - demo QA controls update
- done: removed old manual reserve toggle, added `isThreadSettled` control, wired into layout
- done: preview key now `${threadUuid}:${layoutRemountKey}` so thread switch resets local latch in demo
- files: `apps/web/src/routes/components/_components/conversation.tsx`
