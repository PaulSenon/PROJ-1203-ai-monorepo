[GOAL] Ship chat MVP (strict scope, no feature creep)

[MVP_MUST]
- anonymous account
- basic bot protection
- finish message/conversation UI to target spec
- fix input + draft lifecycle
- fix sidebar live updates
- close core TODOs impacting MVP behavior
- high INP/perf on key interactions (mobile + desktop)
- native-feel drawer on iOS/Android (if vaul insufficient, replace)
- smooth loading strategy (no cascading skeletons/loaders)
- perfect initial conversation anchor scroll on open (not forced bottom)

[MVP_LATER]
- sidebar list virtualization
- conversation message virtualization
- edit feature dropped
- PRD done: client-side message UUID reliability track (client assistant IDs, invisible retry, conversation-level persisted error block)

[ACTIVE_TRACK]
- now: stabilize chat surface + UX correctness first (input/draft/sidebar/scroll/loading)
- conversation architecture pass done (L2/L3 parity w/ message)
- conversation adapter/layout split + pure layout demo scaffold done (minimal props baseline)
- Phase 1 done: new live conversation runtime surface mounted; legacy runtime untouched/dead
- docs corrected: Conversation View Store must be `zustand/vanilla` per grill history
- recovery done: scoped zustand/vanilla view store now live
- Phase 3 core landed: live path now uses conversation-owned source adapters + reconciler; legacy `useMessages` no longer in live path
- Phase 3 parity hardening done: source trim/cache-sync helpers extracted + tested; redundant cache writes skipped
- send-path UI wiring done: prompt input now reflects conversation send state without implying unsupported cancel
- unsupported retry/continue affordances retired from live message UI
- dead message action leftovers retired; live message actions now match send/copy-only support
- Phase 5 verification slice done: vitest now discovers `.test.tsx`; provider/hook integration coverage added for `use-active-conversation`
- docs aligned: regenerate/cancel explicitly legacy-only, manual QA scope is send/load/stream only
- next: manual runtime verification / QA review

[OPEN_DECISIONS]
- bot protection vendor: Turnstile?
- scroll anchor definition: last read vs last user msg?
- drawer replacement lib choice?
- exact "done" metrics per MVP must?
