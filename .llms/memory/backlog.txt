----
[id] env-contract-v2-custom-builder
[status] pending
[captured_at] 2026-02-11
[context] During env refactor, we identified limits in current `@t3-oss/env-core` wrapper for future policy needs (secret/public enforcement, build scope, richer contracts).
[why_deferred] Current priority is finishing boundary-safe env refactor with minimal blast radius; custom env framework would expand scope significantly.
[rough_spec]
- Build custom `createEnv` replacement with explicit sections: `public`, `secret`, `server`, `shared`, `build`.
- Enforce prefix policy for public keys and mandatory secret value policy at type level.
- Preserve input vs output inference API (`InferInputContract`, `InferOutputContract`) and runtime parser ergonomics.
- Provide infra-friendly binding contract projection with optional secret allowance mapping.
[decisions]
- Current implementation keeps `createContractEnv` wrapper around t3-env.
- Secret/public enforcement remains infra-side only for now.
[open_questions]
- Migration path from current wrapper to custom API without breaking app env modules?
- Should build-time contract be first-class in API, or a separate factory?
- How to model resource bindings (R2/DO/Queue/service) alongside scalar envs elegantly?
[next_trigger] Revisit after current env refactor is merged and stable across web/server/infra.
----
[id] ai-elements-full-registry-refresh
[status] parked
[captured_at] 2026-02-10
[context] While planning Streamdown upgrade, we confirmed current message-ui objective can be completed without a full ai-elements registry update.
[why_deferred] Full registry refresh has high blast radius and risks feature creep during chat MVP stabilization.
[rough_spec]
- Re-run ai-elements/shadcn registry updates for impacted components.
- Diff local L1 overrides against upstream.
- Reconcile custom local behavior (attachments, smooth text, design tokens).
[decisions]
- Current scope uses streamdown-first upgrade + selective handpick only if required.
- Prompt-input/model-selector remain untouched in current PRD.
[open_questions]
- Which L1 components should be upgraded together to keep local API consistency?
- Use interactive merge flow or overwrite+manual restore workflow?
[next_trigger] Revisit after streamdown message-ui upgrade ships and stabilizes.
----
[id] client-assistant-id-complex-rollout
[status] parked
[captured_at] 2026-02-09
[context] Complex assistant client-side ID rollout was fully specified but deferred to keep MVP path focused.
[why_deferred] Must first execute the simpler path in `.llms/plan/client-optimistic-assistant-shell/0-prd-simplest-solution.md`. Only escalate to complex rollout if repeated issues show assistant client-side IDs are needed.
[rough_spec]
- Implement `.llms/plan/client-optimistic-assistant-shell/1-prd-complexe-solution-client-side-id.md`.
- Include insert-only create guard, UUID immutability guard, invisible one-retry, and persisted conversation-level error block with retry payload.
[decisions]
- Deferred intentionally, not current MVP critical path.
- Triggered after simplest solution ships, or earlier only on clear repeated need.
[open_questions]
- none
[next_trigger] After `.llms/plan/client-optimistic-assistant-shell/0-prd-simplest-solution.md` is shipped, or if repeated friction confirms assistant client-side IDs are needed.
----
[id] thread-uuid-collision-recovery
[status] parked
[captured_at] 2026-02-09
[context] While scoping message UUID collision handling for optimistic assistant shell, thread UUID collision/recovery was explicitly identified as related but out of current scope.
[why_deferred] Current priority is message flow reliability and MVP chat UX. Thread-level collision handling expands protocol and persistence semantics unnecessarily for current deliverable.
[rough_spec]
- Define deterministic behavior when thread UUID collides on create/navigation/send.
- Add typed error + optional auto-recovery path.
- Ensure no data mix across threads/users and no UI route flicker.
[decisions]
- Not in current PRD scope.
- Message-level UUID handling proceeds independently.
[open_questions]
- Should collision recover by server remap or client reroute?
- Should existing thread be reopened or new thread forced?
- How to preserve draft/input state across recovery?
[next_trigger] Revisit after message UUID project ships and stabilizes in production. (and after `.llms/plan/client-optimistic-assistant-shell/1-prd-complexe-solution-client-side-id.md`)
----
[id] ui-primitives-button-tooltip-unification
[status] pending
[captured_at] 2026-02-08
[context] While finishing chat message footer MVP, we introduced shared primitives (`IconActionButton`, `CopyActionButton`) and noticed button/tooltip design APIs are still duplicated across sidebar, code-block, ai-elements message actions, and other L2/L3 components.
[why_deferred] Current goal is finishing chat MVP behavior/quality. Full primitive unification is cross-cutting refactor and would digress scope now.
[rough_spec]
- Define canonical L2 wrappers around shadcn L1 `Button` and `Tooltip` with app design defaults + API tweaks.
- Make icon/action usage rely on these wrappers (single visual behavior baseline).
- Ensure copy interaction pattern (copy -> check animation) comes from one shared primitive, not repeated logic.
- Migrate existing usages progressively by domain (chat, sidebar, ai-elements wrappers), not big-bang.
[decisions]
- Footer now uses `IconActionButton` and `CopyActionButton` under `ui-custom/primitives/`.
- App custom tooltip wrapper (`ui-custom/tooltip.tsx`) should be the tooltip style baseline.
- This item is not a follow-up for current MVP closure; park until MVP-critical flows are done.
[open_questions]
- Should canonical button wrappers live in `ui-custom/primitives/` or domain folders too?
- Keep `ai-elements/MessageAction` usages or fully replace with app wrappers?
- Migration strategy: codemod + lint rule, or manual incremental conversion?
- Which variants/sizes are truly canonical vs legacy support?
[next_trigger] Start after chat MVP validation passes and no P0/P1 UX regressions remain.
----
